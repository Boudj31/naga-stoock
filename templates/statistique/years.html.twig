{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

<h4>Période du mois de {{data.month}} {{data.year}}</h4><br>
<table class="table">
    <thead>
        <tr>
            <th>Catégorie</th>
            <th>Résulats</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td> Adhésion Gem 3 mois</td>
            <td>{{ memberShipGem ? memberShipGem : '0'}}</td>
        </tr>
        <tr>
            <td> Adhésion RSA</td>
            <td>{{ memberShipRSA ? memberShipRSA : '0'}}</td>
        </tr>
        <tr>
            <td> Adhésion SMIC</td>
            <td>{{ memberShipSMIC ? memberShipSMIC : '0'}}</td>
        </tr>
        <tr>
            <td> Adhésion Bénévole</td>
            <td>{{ memberShipBenevole ? memberShipBenevole : '0'}}</td>
        </tr>
        <tr>
            <td> Adhésion Sup SMIC</td>
            <td>{{ memberShipSup ? memberShipSup : '0'}}</td>
        </tr>
        <tr>
            <td> Adhésion installation Linux </td>
            <td>{{ memberShipLinux ? memberShipLinux : '0'}}</td>
        </tr>
       <tr>
            <td> Ventes</td>
            <td>{{sales ? sales : '0'}}</td>
        </tr>
        <tr>
            <td> Ordinateurs Donnés</td>
            <td>{{computerGive ? ComputerGive : '0'}}</td>
        </tr>
    </tbody>


</table>

      <!--==== Formulaire ====-->
      <p class="desc">Selection par Date</p>
      <div class="card card-padding">

        {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary btn-radius">
                {{ button_label|default('Envoyer !') }}
            </button>
            <a href="{{ path('statistique') }}" class="btn btn-third btn-radius">Retour</a>
        {{ form_end(form) }}

    </div>

 
{% endblock %}