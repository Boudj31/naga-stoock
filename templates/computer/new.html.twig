{% extends 'base.html.twig' %}

{% block title %}Ajouter un matériel{% endblock %}

{% block body %}
     <h1 class="title margin-b">Ajouter un nouveau matériel</h1> 

    <!--==== Formulaire ====-->
    <div class="card card-padding">

        {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary btn-radius">
                {{ button_label|default('Enregistrer !') }}
            </button>
            <a href="{{ path('computer_index') }}" class="btn btn-third btn-radius">{{'btn.back'| trans}}</a>
        {{ form_end(form) }}

    </div>
{% endblock %}
